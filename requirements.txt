# VoCoType Linux Fcitx 5 后端依赖
# 注：pyproject.toml 为标准安装入口；本文件保留用于 pinned 安装

# 音频采集和处理
sounddevice==0.5.2
librosa==0.11.0
soundfile==0.13.1

# 语音识别核心
funasr_onnx==0.4.1
jieba==0.42.1
PyYAML>=6.0.0

# 模型下载 (推荐)
modelscope==1.30.0

# 远程大模型流式润色
litellm>=1.65.0
# SOCKS 代理支持（httpx / litellm 在代理环境下需要）
socksio>=1.0.0

# 说明：
# - 默认使用 ONNX 路线；长句润色统一通过 litellm 调用 OpenAI-compatible 流式接口
# - Rime 相关的 pyrime 由安装脚本按需安装
